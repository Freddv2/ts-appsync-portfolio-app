{
  "name": "place-order",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc"
  },
  "dependencies": {
    "aws-appsync": "^4.0.1",
    "aws-sdk": "^2.798.0",
    "cross-fetch": "^3.0.6",
    "graphql-tag": "^2.11.0",
    "ksuid": "^2.0.0"
  }
}
